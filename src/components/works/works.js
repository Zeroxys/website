import React from 'react'

const Works = () => {

  let styles = {
    worksContent : {
      display:'flex',
      justifyContent: 'center',
      alignItems: 'center',
      color: 'black',
      height : 600
    }
  }

  return (
    <section style={styles.worksContent}>
      works
    </section>
  )
}

export default Works
